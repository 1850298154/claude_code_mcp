# Config Module

> 配置模块

## 子模块

| 文件 | 描述 | Bash 定位 |
|------|------|-----------|
| `paths.py` | ClaudeMetaPaths 数据结构定义 | `find . -name "paths.py" -path "*/claude_meta/*"` |

## 结构

```
config/
├── README.md              # 本文件
└── paths.py               # ClaudeMetaPaths 数据结构定义
```
