# Claude Types Module

> Claude 相关类型定义

## 类型定义

| 文件 | 类型 | Bash 定位 |
|------|------|-----------|
| `message.py` | Message 数据结构 | `find . -name "message.py" -path "*/claude/*"` |
| `conversation.py` | Conversation 数据结构 | `find . -name "conversation.py" -path "*/claude/*"` |
| `session.py` | Session 数据结构 | `find . -name "session.py" -path "*/claude/*"` |

## 结构

```
claude/
├── README.md              # 本文件
├── message.py             # Message 数据结构
├── conversation.py        # Conversation 数据结构
└── session.py             # Session 数据结构
```
